{
  "component": "console-mcp",
  "environment": "dev",
  "required": [
    {
      "env": "CONSOLE_MCP_BEARER_TOKEN",
      "secret": "/secret/dev/console-mcp/bearer-token"
    }
  ],
  "optional": [
    {
      "env": "CLOUDFLARE_API_TOKEN",
      "secret": "/secret/dev/cloudflare/api-token",
      "purpose": "Required only when the ask tool uses Cloudflare AI Gateway."
    }
  ]
}
